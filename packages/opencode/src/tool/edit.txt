Replace an exact string in a file.

- Read the file first; the edit fails otherwise.
- oldString must match the file exactly, including indentation, and must occur once. Copy it from the Read output after the "N: " line-number prefix; never include the prefix.
- If oldString occurs more than once, include more surrounding lines, or set replaceAll to change every occurrence (for renames).
- Prefer editing existing files over writing new ones. No emojis unless asked.
