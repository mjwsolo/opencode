Search file contents with a regular expression. Returns file paths and line numbers with the matching lines. Filter by file pattern with include (for example "*.py" or "*.{ts,tsx}"). To count matches, run rg in bash.
